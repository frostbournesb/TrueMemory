"""Claude Code hook entry points for neuromem-ingest.

These modules live inside the package namespace so they ship cleanly
in the built wheel. Each one is invoked directly by Claude Code via
``python -m neuromem.ingest.hooks.<name>``.
"""
